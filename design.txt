Create a website for people to guess a soccer match result. Split into different tournaments, and get actual data from the sources, which I added inside the source.txt with the site and api keys. User can sign up and login with username and passwords, select the tournament, see the fixtures, open a match, select who wins. They can also see the history of how they did throughout the tournament, open an old match and see the results, how others did, and rankings for that tournament.
Find a good place to keep the infos for the users (like history and such), i can think of using a spreadsheets for all the username and passwords (they don't really matter that much, no need to keep secure, note that when someone is creating an account)
The matches data should be fetch once everyday and once after a match finishes (maybe 3 hours later). Only fetch it once, as the number of requests aren't that many. You can try to find another way to do it. I only really need the fixtures and results (no need for live scores).
I don't need live odds, add a refresh for the odds and have it at 5 minutes interval (which means if someone update the odds, you should save when the odds was pressed and keep them, prevent others from updating the odds later). Get from draftKings and 1xbet
The 2 sites might have differences in the package they sent back, you need to set it up so that the matches line up (correct odds for matches). If you can't find a way then just remove the odds.
Create the site so i can upload it on github and have friends use them. Keep the apis keys hidden.